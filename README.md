# CZ4031 Project 2

To view the GUI window, run `project.py`.

Due to limitations with the GUI, it is recommended to run the project on a larger screen as the window will be cut off on smaller screens, cutting-off the bottom of the annotation box and hiding the graphical tree view.
